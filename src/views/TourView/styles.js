import {
  StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
  tourListContainer: {
    display: 'flex',
    flex: 1,
    paddingTop: 5,
    paddingRight: 10,
    paddingLeft: 10,
    paddingBottom: 5
  },
  noItems: {
    textAlign: 'center',
    fontSize: 18,
    fontWeight: '300'
  }
})

export default styles

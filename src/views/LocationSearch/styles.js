import {
  StyleSheet
} from 'react-native'

const styles = StyleSheet.create({
  locationSearchContainer: {
    display: 'flex',
    flex: 1,
    alignItems: 'center',
    paddingTop: 20,
    paddingLeft: 10,
    paddingRight: 10,
    paddingBottom: 20
  }
})

export default styles
